<Window x:Class="EncryptNote.Views.EnterPasswordWindow"
        x:Name="EnterPasswordWindowUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EncryptNote.Views"
        xmlns:vm="clr-namespace:EncryptNote.ViewModels"
        mc:Ignorable="d"
        Title="EnterPasswordWindow" Height="150" Width="450">
    <Window.DataContext>
        <vm:EnterPasswordWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40*"/>
            <ColumnDefinition Width="60*"/>
        </Grid.ColumnDefinitions>
        <Label Margin="15" Content="Enter master password"/>
        <PasswordBox Margin="17" Grid.Column="1" x:Name="pwdBox"/>
        <Button Margin="15" Grid.Row="1" Grid.ColumnSpan="2" Content="Enter"
                Command="{Binding EnterCommand}" CommandParameter="{Binding ElementName=EnterPasswordWindowUI}"/>
    </Grid>
</Window>
